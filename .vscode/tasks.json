{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run codegen",
            "type": "shell",
            "command": "dart run build_runner watch --delete-conflicting-outputs"
        }
    ]
}